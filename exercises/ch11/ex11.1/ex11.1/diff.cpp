#include <iostream>
#include <map>
#include <vector>

using std::cin;
using std::cout;
using std::endl;

int main()
{
	map<int, unsigned int> numbers;
	// a map has a key-value pair (is an associative container)
	// where each key is assigned to one value, both types are given in definition

	vector<int> vint;
	// a vector stores a sequence of single values (sequence container)
}


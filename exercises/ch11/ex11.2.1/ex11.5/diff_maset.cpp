#include <iostream>
#include <set>
#include <map>

using std::cin;
using std::cout;
using std::endl;

int main()
{
	std::set<int> sint;
	//set only contains a key, so its used in storing singular values
	std::map<int, int> mint;
	//map contians both a key and an associated value, so it is used when you need two seperate values
	// which associate with each other
}


#include <iostream>
#include <list>
#include <set>

using std::cin;
using std::cout;
using std::endl;

int main()
{
	std::list<int> lint;
	//a list can take multiple copies of the same value, and they can be accessed by position or iterator

	std::set<int> sint;
	//a set can only take unique values, and they can be accessed by name;
}


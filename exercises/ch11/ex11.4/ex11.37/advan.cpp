#include <map>
#include <string>
#include <iostream>

using std::unordered_map;
using std::map;
using std::string;
using std::cin;
using std::cout;
using std::endl;

int main()
{
	unordered_map<string, int> uses;
	// can be found in order of insertion. Uses hashing

	map<string, int> ord_uses;
	// pre ordered, values can be quickly found and thier positions understood

}

